fetaures:

add jupyter message

NO - Add optional checkbox to have the user specify if the env is for a Jupyter notebook. In this case, install jupyter package into the env during creation.
NO - need form validation at point of submission for the env name - (done)no we don't, just send forward the backend script's input validation
DONE - make the chart column titles look better

bugs:


FIXED - if you delte all of your environments using the frontend, the last delete breaks it. 
